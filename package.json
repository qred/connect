{
  "name": "qred-connect-api",
  "version": "1.0.0",
  "dependencies": {
    "swagger-repo": "^2.0.0-rc.6"
  },
  "private": true,
  "scripts": {
    "start": "swagger-repo serve",
    "build": "swagger-repo build -o docs",
    "test": "swagger-repo validate",
    "gh-pages": "swagger-repo gh-pages"
  }
}
